package controller;

import model.Person;

public class EmployeeCtrImp extends PersonCtrImp {
    @Override
    public void show(Person person) {
        super.show(person);
    }

    @Override
    public Person addFromKeyBoard() {
        return super.addFromKeyBoard();
    }
}
